{
  "name": "cocicp-gastos",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "node scripts/check-syntax.mjs",
    "lint": "node scripts/check-syntax.mjs",
    "build:pages": "node scripts/build-pages.mjs",
    "verify:pages": "node scripts/verify-pages.mjs",
    "preflight": "npm run test && npm run check && npm run build:pages && npm run verify:pages && wrangler deploy --dry-run",
    "deploy:worker": "npm run test && npm run check && wrangler deploy",
    "deploy:pages": "npm run test && npm run check && npm run build:pages && npm run verify:pages && wrangler pages deploy dist-pages --project-name=cocicp-gastos-ui"
  },
  "devDependencies": {
    "vitest": "^2.1.9",
    "wrangler": "^4"
  }
}
